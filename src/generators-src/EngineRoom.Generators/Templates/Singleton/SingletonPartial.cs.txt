partial class %%ClassName%%%%BaseList%%
{
    public static %%InterfaceName%% Create()
    {
        var obj = new global::UnityEngine.GameObject(nameof(%%ClassName%%));
        return obj.AddComponent<%%ClassName%%>();
    }
    
    private void Awake()
    {
        var existing = %%InterfaceName%%.Instance as global::UnityEngine.Object;
        if (existing != null && existing != this)
        {
            global::UnityEngine.Object.Destroy(gameObject);
            return;
        }

        %%DontDestroyLine%%
        %%InterfaceName%%.Instance = this;
        OnAwake();
    }

    partial void OnAwake();
}
